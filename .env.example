# Örnek ortam değişkenleri (gerçek değerleri buraya yazma, .env kullan)

# --- Supabase Postgres ---
# Dashboard → Project Settings → Database → Connection string
# App / Vercel (Transaction pooler, port 6543):
DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Migrate için Direct connection (port 5432):
DIRECT_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"

AUTH_SECRET="replace_me"

ADMIN_EMAIL="info@example.com"
ADMIN_PASSWORD_HASH_B64="replace_me"

# Cloudinary (kalıcı görsel upload)
CLOUDINARY_CLOUD_NAME="replace_me"
CLOUDINARY_API_KEY="replace_me"
CLOUDINARY_API_SECRET="replace_me"

# Genel — kanonik site adresi (sitemap, metadata, isteğe bağlı istemci)
NEXT_PUBLIC_SITE_URL=https://www.mimarsametalp.com

# Ana sayfa kahraman videosu — doluysa tek URL (CDN/MP4 vb.), boşsa `public/videos/kendi-hero.mp4`
# NEXT_PUBLIC_HOME_HERO_VIDEO_URL=
# `kendi-hero.webm` (VP9) ekledikten sonra modern tarayıcılarda daha küçük dosya için üretimde açın:
# NEXT_PUBLIC_HOME_HERO_WEBM=1
# Örnek: ffmpeg -y -i public/videos/kendi-hero.mp4 -an -c:v libvpx-vp9 -crf 34 -b:v 0 public/videos/kendi-hero.webm
# Video yüklenene kadar kapak görseli (boşsa varsayılan `public/videos/kendi-hero-poster.webp`)
# NEXT_PUBLIC_HOME_HERO_POSTER_URL=/videos/kendi-hero-poster.webp

# Auth.js v5 — üretimde zorunlu: openssl rand -base64 32
AUTH_SECRET=
# Oturum / OAuth yönlendirmeleri için taban URL (https ile, sonda / yok)
AUTH_URL=https://www.mimarsametalp.com
NEXTAUTH_URL=https://www.mimarsametalp.com

ADMIN_EMAIL=admin@mimarsametalp.com
# node scripts/hash-admin-password.mjs "Şifren" → ADMIN_PASSWORD_HASH_B64 satırını kopyala
ADMIN_PASSWORD_HASH_B64=

# İletişim (Resend) — üretimde RESEND_API_KEY şart
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CONTACT_INBOX_EMAIL=info@mimarsametalp.com

# Müşteri portalı SMS (şimdilik stub — boş bırakılabilir)
# SMS_PROVIDER=netgsm
# SMS_PROVIDER_USER=
# SMS_PROVIDER_PASS=
# SMS_PROVIDER_HEADER=
